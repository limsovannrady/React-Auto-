services:
  - type: worker
    name: telegram-auto-reaction-bot
    runtime: docker
    region: singapore
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
    numInstances: 1
    autoDeploy: false